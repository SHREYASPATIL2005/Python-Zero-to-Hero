d = {}

name = input("Enter friend's name: ")
lang = input("Enter friend's language: ")
d.update({name: lang}) # This line updates the dictionary 'd' by adding a new key-value pair, where the key is the friend's name and the value is their language. The update() method is used to add the new entry to the dictionary.

name = input("Enter friend's name: ")
lang = input("Enter friend's language: ")
d.update({name: lang}) # This line updates the dictionary 'd' by adding a new key-value pair, where the key is the friend's name and the value is their language. The update() method is used to add the new entry to the dictionary.

name = input("Enter friend's name: ")
lang = input("Enter friend's language: ")
d.update({name: lang}) # This line updates the dictionary 'd' by adding a new key-value pair, where the key is the friend's name and the value is their language. The update() method is used to add the new entry to the dictionary.

name = input("Enter friend's name: ")
lang = input("Enter friend's language: ")
d.update({name: lang}) # This line updates the dictionary 'd' by adding a new key-value pair, where the key is the friend's name and the value is their language. The update() method is used to add the new entry to the dictionary.

print(d) # This line prints the dictionary 'd', which will contain all the key-value pairs of friends' names and their corresponding languages that were entered by the user. The output will be a dictionary with the friends' names as keys and their languages as values.



# If Keys are same, then the value will be updated with the new value. The dictionary will not have duplicate keys, and the latest value provided for a key will overwrite any previous value associated with that key.
# If values are same, then the dictionary will still have unique keys, and each key will be associated with its respective value. The values can be the same for different keys, but the keys themselves must be unique within the dictionary.